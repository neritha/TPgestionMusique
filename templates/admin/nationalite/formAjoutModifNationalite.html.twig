{% extends 'base.html.twig' %}

{% block title %}
{% if (app.request.attributes.get('_route')=="admin_nationalite_ajout") %}
    Ajout
{% else %}
    modification
{% endif %}
d'une nationalite{% endblock %}

{% block body %}
<h1 clas="pt-2 text-center">
{% if (app.request.attributes.get('_route')=="admin_nationalite_ajout") %}
    Ajout
{% else %}
    Modification
{% endif %}
d'une nationalite</h1>

{{form_start(formNationalite,{'attr':{'novalidate':'novalidate'}})}}

{{form_row(formNationalite.libelle)}}
{{form_row(formNationalite.drapeau)}}


<div class="row">
    <div class="col"><a href=" {{ path('admin_nationalites')}} "class="btn btn-danger btn-block"><i class="fas fa-undo-alt"></i> Annuler</a>
</div>
    <div class="col"><input type="submit" class="btn btn-info btn-block" value=
    {% if (app.request.attributes.get('_route')=="admin_nationalite_ajout") %}
        "Ajouter"
    {% else %}
        "Modifier"
    {% endif %}
    ></div>
</div>

{{form_end(formNationalite)}}
    
{% endblock %}
